#include "main.h"

/**
 * _puts - a function that prints a string,
 * followed by a new line, to stout.
 * @str: an input string
 * Return: string
 */
void _puts(char *str)
{
	(%s, strrev(str));
	return 0;
}
