#include "main.h"

/**
 * check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "I do not fear computers. I fear lack of them - Issac Asimov";
	print_rev(str);
	return (0);
}
