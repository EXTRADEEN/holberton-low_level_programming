#include "main.h"

/**
 * *_strcat - is a function that concatenates two strings
 * @src: copy from
 * @dest: paste to
 * Return: dest with new result
 */
char *_strcat(char *dest, char *src)
{
	int i, n;

	for (i = 0; dest[i] != '\0'; i++)
	{
	}

	for (n = 0; (dest[i + n] = *src++) != '\0'; n++)
	{
	}
	return (dest);
}
